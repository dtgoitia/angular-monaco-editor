import { Component } from '@angular/core';
import { DialogueSchemaService } from './../services/dialogue-schema.service';
// import { Observable } from 'rxjs/Rx';

@Component({
  selector: 'app-dialogue-schema',
  template: `
    <h1>Angular 5 HttpClient Demo Component</h1>
    <h2>Foods</h2>
    `
})
export class DialogueSchemaComponent {

  public foods;

  // constructor(private _dialogueSchemaService: DialogueSchemaComponent) { }
}
